import React from 'react'

const EditForm = (props: any) => {
  const { openEditModal, handleEditClose, training } = props;
  console.log(training)

  return (
    <div>
      
    </div>
  )
}

export default EditForm
